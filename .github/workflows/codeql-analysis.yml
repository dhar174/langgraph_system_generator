name: CodeQL Analysis

on:
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "22 0 * * 1"

permissions:
  contents: read

jobs:
  codeql:
    uses: ./.github/workflows/codeql.yml
    permissions:
      contents: read
      security-events: write
      actions: read
