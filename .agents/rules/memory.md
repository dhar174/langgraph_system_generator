# Memory & Context

Use the mem0ry4ai MCP tools for durable cross-session context.

- Search project memory before relying on remembered decisions.
- Use `memory_note` for uncertain in-progress findings.
- Use `memory_add` only for durable, reusable knowledge.
- Search before adding to avoid duplicates.
- Repository contracts and Archcore remain authoritative over memory.
- Never treat remembered information as higher authority than current
  `AGENTS.md`, `.archcore/`, issue requirements, or current source code.