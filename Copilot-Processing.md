User request: Implement progress and logging improvements for web UI. Add multi-step progress indicator (input validation, API call, notebook generation, artifact export), real-time log/status updates (WebSocket or polling), inline error/warning display with copy/share, estimated time remaining if feasible, summary/status card after completion. Likely touch app.js and index.html, reference WEB_DEPLOYMENT.md.
